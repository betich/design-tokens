# Public base URL of the published registry.
#
# Used by `pnpm build:registry` to rewrite this repository's own registry
# dependencies into absolute URLs, so `shadcn add` resolves them here rather
# than against the upstream shadcn registry.
#
# Leave unset for local development; dependencies stay as bare names.
REGISTRY_URL=https://example.com
